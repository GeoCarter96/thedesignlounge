/** @type {import('next').NextConfig} */
const nextConfig = {
  // Turbopack handles standard CSS imports automatically
  experimental: {
    // If you had any tailwind-specific experimental flags, remove them here
  },
};

export default nextConfig;
